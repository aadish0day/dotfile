[global]
    # Font and typography
    # font = "Noto Sans 10"
    font = Monospace 10

    
    # Geometry and positioning
    width = 300
    height = (0, 300)
    offset = 30x50
    origin = top-right
    
    # Notification behavior
    show_age_threshold = 60
    stack_duplicates = true
    hide_duplicate_count = false
    
    # Formatting
    markup = full
    format = "<b>%s</b>\n%b"
    alignment = left
    
    # Timeouts and urgency
    timeout = 10
    
    # Visual settings
    transparency = 10
    frame_width = 2
    corner_radius = 8
    
    # Color scheme
    background = "#2E3440"
    foreground = "#D8DEE9"
    frame_color = "#5E81AC"

[urgency_low]
    background = "#3B4252"
    foreground = "#D8DEE9"
    frame_color = "#4C566A"

[urgency_normal]
    background = "#434C5E"
    foreground = "#ECEFF4"
    frame_color = "#5E81AC"

[urgency_critical]
    background = "#BF616A"
    foreground = "#ECEFF4"
    frame_color = "#D08770"
    timeout = 0

[shortcuts]
    close = ctrl+space
    close_all = ctrl+shift+space
    context = ctrl+shift+period
